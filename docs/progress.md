# coja — build progress

Source of truth: [design.md](design.md). Choices where the design is silent: [decisions.md](decisions.md).

Legend: `[ ]` not started · `[~]` in progress · `[x]` done and validated

## Stage 1 — Scaffold
- [x] pnpm monorepo: `server/` (package `coja`, bin) + `web/` (Vite React)
- [x] `coja` bin starts server, serves built UI from package, opens browser
- [x] typecheck + lint + unit tests wired at root (`pnpm check`)
- [x] Validation: build once, `node server/bin/coja.js` serves UI in browser (2026-09-04, health + SPA fallback verified in browser)

## Stage 2 — Git layer + Forge + gh auth
- [x] `git` plumbing wrapper (show / ls-tree / grep / diff / log / blame / fetch / merge-base) — no worktree/checkout anywhere (145 unit tests; live fetch of fixture PR verified)
- [x] `Forge` interface + GitHub GraphQL implementation, token from `gh auth token` (63 unit tests; 18-step live check against fixture PR: pending comment, edit, viewed, reply, discard)
- [x] Setup screen: `gh auth status` surfaced, API key → keychain + validation, skippable (browser: gh green, bogus key → "That key was rejected by OpenAI.", Skip → Projects)
- [x] Validation: unit tests against a temp git repo ✔; setup screen in browser ✔

## Stage 3 — Projects → PR list → diff view
- [x] Add project by local clone path (origin auto-detected) and by `owner/repo` (bare clone into app storage) — both via API; local path also via the UI form (browser)
- [x] PR list: title, author, branch, updated-at, my review state (browser: 'Commented' badge on #1)
- [x] PR screen: background fetch of `pull/<n>/head`, metadata immediately (browser: Overview renders at once, pill Fetching→Ready)
- [x] Continuous lazily-rendered diff (`@pierre/diffs` CodeView), stacked/split toggle — jump-scroll fixed (row metrics aligned with the virtual layout, zero-height heal, scroll retrier, highlighter preload); browser: six consecutive jumps incl. into/out of the 2400-line file all land at top 0
- [x] Changed-files tree (`@pierre/trees`) with change types, comment badges, viewed checkmarks; Overview entry (browser)
- [x] Validation: browser run against fixture PR (#1 and bot PR #2)

## Stage 4 — Review sync
- [x] Line/range comment → pending review (browser on #1; `gh api`: review PENDING, comment PENDING)
- [x] Reply to existing thread (browser: joins the open pending review with Pending badge; with no pending review it publishes immediately — both verified via `gh api`)
- [x] Viewed checkmark round-trips via `markFileAsViewed` (browser checkbox → `viewerViewedState: VIEWED` via `gh api`)
- [x] Submit: Comment (#1, publishes 2 pending comments) / Approve (#2) / Request changes (#2) — each verified via `gh api` (state, body, comments SUBMITTED). Note: GitHub forbids Approve/Request-changes on your own PR, hence the bot-authored PR #2 (workflow in the fixture repo).
- [x] Validation: browser + `gh api` verification against fixture PR, repeated from a clean pending state

## Stage 5 — AI panel
- [x] Agent loop (Vercel AI SDK v7), exactly six read-only tools (`server/src/ai`; static invariant test forbids forge/child_process/fs/network imports; 45 tests incl. mock-model streaming)
- [x] Ask AI → context chip (visible, expandable, removable) — browser: `src/auth/session.ts:14–15 @ head`, expands to the exact text, × removes
- [~] Tool calls + results rendered in chat; `file:line` citations scroll diff — implemented + unit-tested (tool cards, remark citation plugin → bridge.scrollToLine); live browser check BLOCKED on an API key
- [~] Conversations persisted in SQLite; survive restart; new chat / history per PR — server persists on `onEnd` (tested); restart round-trip BLOCKED on an API key
- [x] Model picker; "what does the AI see?" affordance — browser: dialog shows the verbatim system prompt + six tools; no-provider notice links to Setup and the review tool keeps working
- [ ] Validation: browser with real provider key (BLOCKED until `OPENAI_API_KEY` provided)

## Cross-cutting
- [x] E2E fixture repo `coja-e2e-fixture` with PR #1 (adds, edits, rename, large file) — see e2e-fixture.md
- [x] Invariant audit — round 1 (Opus, server stages 2–4): Invariant 1 PASS, Invariant 2 PASS structurally; CSRF/DNS-rebinding gap (C1) + M3–M6/L7–L14 fixed and guard verified over HTTP. Round 2 (Opus, AI layer + guard + web): Invariant 1 PASS, Invariant 2 PASS, guard closes the cross-origin hole (with documented caveats); all findings fixed (H1 remote images, M2 CSP, M3 wildcard bind, M4 chat caps/pruning, M5 rename-aware Ask AI, L6–L11) and re-verified (544 tests, CSP headers live).

## Resume notes (written 2026-09-04 23:55, before a usage-limit pause)

- Research reports (ground truth for library APIs) live in the session scratchpad: `research-pierre.md`, `research-github.md`, `research-aisdk.md`, `research-infra.md` under
  `/private/tmp/claude-501/-Users-ayush-porwal-Documents-workspace-coja/afd08bb9-0e4d-4d81-b350-d51ce84dc70e/scratchpad/`.
- Contracts written and committed: `server/src/shared/api.ts` (wire types + routes), `server/src/forge/forge.ts` (Forge interface), `server/src/routes/http.ts` (error mapping), `server/src/projects/store.ts`, `server/src/db.ts`, `server/src/context.ts`, `web/src/api/client.ts`.
- Dependencies pre-installed: server `ai@7.0.92 @ai-sdk/openai@4.0.58 @ai-sdk/anthropic@4.0.49 zod@4.5.4 @napi-rs/keyring@2.0.0`; web `@pierre/diffs@1.4.0 @pierre/trees@1.0.0-beta.6 ai @ai-sdk/react zod react-markdown remark-gfm`.
- Parallel implementation agents dispatched next (each confined to its paths): A git layer (`server/src/git`, `server/src/pr`, `server/src/projects/add.ts`, `routes/projects.ts`, `routes/git.ts`); B GitHub forge (`server/src/forge/*`, `routes/pulls.ts`, `routes/review.ts`); C web shell (`web/src/**` minus `pr/`); D secrets + setup (`server/src/secrets`, `routes/setup.ts`); E PR review screen (`web/src/pr/**`).
- After agents finish: wire `registerXRoutes` in `server/src/app.ts` + `cli.ts` (createContext, GitHubForge, PrFetcher, secret store), run `pnpm check`, build, start, browser-validate Stages 2–4 against the fixture PR, Opus invariant audit, then Stage 5 (AI panel: server `server/src/ai/**` + `routes/chat.ts`; web `web/src/pr/ai/**`).
- `OPENAI_API_KEY` is absent in the environment: final AI-panel validation needs a key from the user.
- **2026-09-05 00:0x — paused on user request (usage limits).** All five implementation agents (A–E) were STOPPED mid-work to save credits. Their partial, uncommitted files may exist under `server/src/{git,pr,projects,forge,secrets,routes}` and `web/src/**`. On resume (cron fires 04:24): run `git status`, inspect what each agent left, then re-dispatch agents A–E with the same briefs plus "build on the partial files already present; review them first". Nothing from A–E is committed.

## Final state (2026-09-05)

- Acceptance criteria 1–6 and 8 validated in the browser / on GitHub. Criterion 7 (AI panel) is implemented and unit-tested end-to-end with a mock model; the live run needs an `OPENAI_API_KEY` (or Anthropic key), which was absent from the environment.
- Known environment quirk (not an app bug): the very first automated click after a page navigation in the Chrome automation tool delivers no DOM events; a human click works. Documented here so it is not chased again.
- **Validation servers (session preference, 2026-09-05): use the user's running instance on port 4321 — do not start a `coja` process for browser validation and do not leave one behind.** The user keeps `coja` running on 4321; since the built UI is served from `server/public` per-request, a fresh `pnpm build` is picked up by the running process, but *server-code* changes need the user to restart it. Verify new routes are live (`curl :4321/api/setup/status`) before validating against the instance.

## UI/UX iteration (2026-09-05, this run)

UI-only pass: no server API, git-plumbing, Forge or AI-tool-surface changes; `pnpm check` green (567 tests, +23 vs. the pre-run 544) and `pnpm build` green.

- [x] **Task 1 — Collapsible panels, VS Code style.** `PanelToggles` group on the left of the review top bar (filled-half rectangle glyphs; left = file tree, right = AI panel); `usePanelShortcuts` wires Cmd/Ctrl+B (tree) and Cmd/Ctrl+Shift+B (AI) with tooltips showing the chord; visibility persists via `usePersistedState` (`coja.fileTreePanelOpen`, existing `coja.aiPanelOpen`); collapsed toggles show attention badges (tree: thread count; AI: context chips waiting in the composer, plumbed via `Composer.onChipsChange`). Browser: toggles + chords both directions, diff reflows (main width 772↔1152), state survives reload.
- [x] **Panel widths are draggable** (user request): `PanelResizeHandle` between each panel and the diff — pointer drag (with `coja-resizing` body lock), arrow/Home/End keyboard resize (AT mirror: `aria-valuenow`), double-click-free; widths persist (`coja.treePanelWidth` 200–460 px, `coja.aiPanelWidth` 300–640 px). Widths are CSS-capped at `calc(50vw - 140px)` so a narrow window can never squeeze the diff to zero (verified: 880 px viewport → 300/300/268, no horizontal overflow).
- [x] **Task 2 — Theme system ported from t3code.** `web/src/themes/`: `palettes.ts` (five t3code palettes verbatim in oklch — Mulberry (né T3 Chat, renamed per user), Grove, Ocean, Ember, Iris — each with light+dark variants, plus coja's own `syntax` token ramps per palette), `registry.ts` (preference `{palette, appearance: system|light|dark}`, `coja.theme` storage, OS default), `apply.ts` (one `applyTheme()` writes `--coja-*` chrome vars + `--diffs-*` token/tint overrides on `:root`), `diffTheme.ts` (registers `coja-light`/`coja-dark` Shiki CSS-variable themes — token colours are `var(--diffs-token-*)`, so palette switches are pure CSS, no re-tokenization), `treeTheme.ts` (`themeToTreeStyles` maps palette → FileTree), `ThemeContext`/`ThemeMenu`/`ThemePicker`/`ThemeSwatch`. Quick switcher in every screen header; full picker in Setup; `Auto` follows `prefers-color-scheme` until the user picks. `index.css` maps tokens to Tailwind utilities (`@theme inline`) and pins the `dark:` variant to `[data-coja-appearance]`, so existing utilities follow the app appearance, not the OS. Browser: one switch restyles chrome, diffs, tree and chat together; choice survives reload and server restart; default follows OS.
- [x] **Task 3 — Design pass on all four screens.** Whole codebase converted from raw zinc/blue/green classes to the semantic token vocabulary (codemod + hand cleanup; zero hardcoded palette colours left outside tests); dropped the all-caps eyebrow labels (Overview sections, sidebar Files) — a generated-page tell; top bar restructured (toggles | divider | back | title, author, branch chip, GitHub link | fetch pill, stacked/split, theme menu, Review); solid changed-row fills using t3code's verbatim `DIFF_SURFACE_THEME` formulas (sRGB mixes toward the full-strength ok/danger colours — adopted after the user found a first custom `lab`-mix attempt too faint).
- [x] **Collapsible files in the diff** (user request): chevron in every file header (`aria-expanded`) collapses the file to its header (CodeView `file` item with empty contents — header-only render); jumping to a collapsed file from the tree/citations auto-expands it first.
- [x] **Validation.** Browser-driven against the fixture PR on port 4923 (4321 was left free for the user's instance): panel toggles/shortcuts/persistence, drag resize both handles + reload persistence, live theme switching (Mulberry dark ↔ Grove light + appearance control), solid fills, file collapse + auto-expand. Screenshots: `docs/screenshots/{mulberry-dark,grove-light}{,-narrow}/` — all four screens, wide (1440×900) and narrow (880×820).
- AI-panel live chat still blocked on a provider key (unchanged from last run; everything else of the panel — theming, layout, composer, badges — verified).

## Follow-up UI round (2026-09-05, later)

- [x] **Selection popover button wrap** — the Comment / Ask AI buttons shrank below their content width when a long path filled the toolbar ("Ask AI" wrapped to two lines). The path label is now the only shrinking element (`min-w-0 truncate`); buttons are `shrink-0 whitespace-nowrap`. Verified with the popover squeezed to ~330px.
- [x] **Per-theme diff contrast (user challenge: "are all themes sharing diff colors?").** Researched t3code: deletions derive from each palette's `error` role (`--destructive: var(--error)` — per-theme), additions use one fixed emerald for all palettes; appearance contrast comes from the mix ratios (dark = 70 % background / 30 % colour, which keeps fills dark so near-white text stays readable). coja already matched that structure — `danger` per palette, `ok` fixed, code background/ink per palette — but nothing *proved* it. New `web/src/themes/contrast.test.ts` computes the real fills (oklch → sRGB → CSS-spec `color-mix` → WCAG ratio, plus OKLab ΔL for band visibility) for every palette × appearance and enforces: diff text ≥ 4.5:1 on both fills, bands ≥ 1.3:1 visible and ≥ 0.06 ΔL from the code surface, context rows ≤ 0.03 ΔL. It caught exactly one failure — Mulberry light, whose code ink (0.445, t3code's own value and the lightest of the five) reached only 3.7:1 on the green fill; darkened that palette's light `codeInk` to 0.32. All 10 combinations now pass; future palettes that fail fail the suite.
- [x] **Thin clue scrollbars + tree scrollbar (user request).** Global thin scrollbars (`scrollbar-width: thin` + a quarter-strength ink thumb via `scrollbar-color`, with `::-webkit-scrollbar` rules for older engines). The Pierre file tree's own thumb is transparent-until-hovered — invisible even while scrolling — so Sidebar now injects tree-scoped CSS through the component's `unsafeCSS` option forcing the thumb to the same themed ink at all times. Note for future sessions: the in-app test browser ignores `::-webkit-scrollbar` and paints overlay thumbs only mid-scroll, so at-rest visibility was verified via computed styles (thumb colour no longer `transparent`, `scrollbar-width: thin` present); on the user's Chrome, which paints overlay thumbs, the tree now shows the same thin bar as the AI panel.
- [x] **Marking a file Viewed collapses it (user request); un-viewing expands it.** First implementation collapsed synchronously in the checkbox's change handler and crashed the whole screen — `@pierre/diffs` throws "rendered a different file than its prepared layout" when the item set mutates inside the checkbox's own event dispatch (React unmounts the root; the blank-page reports earlier in this round were this). Final implementation defers the collapse/expand to a `setTimeout(…, 0)` task boundary inside `onToggleViewed`; the virtualizer's prepared layout stays consistent and the intent survives the mutation's stale-read refetches. Browser-verified both directions, zero errors across repeated toggles; chevron expand/collapse and jump-into-collapsed-file auto-expand unchanged.
- [x] **Split-diff column resize (user request).** A `SplitDivider` overlay sits on the split boundary: drag (with the `coja-resizing` body lock), arrow keys (Shift for larger steps), Home/End; ratio persists per user (`coja.splitLeftPct`, 20–80 %). The boundary lives in the `--coja-split-left` CSS variable on the diff root, consumed two ways: the library's split grid reads it through a *static* `unsafeCSS` override (`grid-template-columns: var(--coja-split-left) calc(100% - var(--coja-split-left))` — constant string, so dragging never triggers item re-renders), and the divider handle positions itself from the same variable, so drags are pure CSS writes. Divider renders only in split mode; keyboard-operable (`role=separator` with AT values). Browser-verified: drag 50 %→32 %, keyboard ±2 %/±5 %, persistence across reload.
- [x] **Virtualizer crash hardening (from the user-reported collapse crash).** Collapse/expand swaps items under `@pierre/diffs`, whose async worker renders can claim a prepared layout for the old item — throwing "rendered a different file than its prepared layout" and unmounting the app. Layers: (1) the collapsed variant lives under a distinct item id (`collapsedItemId`), so the library mounts a fresh virtualized file per state instead of re-preparing in place; (2) every collapsed-set update is deferred past two animation frames (`scheduleCollapsed`); (3) a `VirtualizerBoundary` remounts the CodeView on residual throws, restoring scroll position, with a crash-loop fallback after three failures in 5 s. Stress-verified: the user's two-file collapse repro plus rapid multi-file expand/collapse sequences produce zero virtualizer errors.

## ChatGPT subscription provider (2026-09-05, this run)

Server-side feature plus the smallest web surface; the agent loop, the six tools, and `routes/chat.ts`'s model resolution are behaviour-identical (the chat handler gained only an optional `subscription` pass-through). `pnpm check` green (651 tests, +84) and `pnpm build` green.

- [x] **Research doc** — `docs/research/chatgpt-subscription-auth.md`: mechanism (Codex CLI OAuth client, loopback PKCE vs device flow, tokens/refresh/quarantine, Codex Responses transport + SSE), prior art read at source level (opencode's codex plugin, `chatgpt-oauth` PROTOCOL.md + client + AI SDK bridge, the Codelynx writeup, official Codex auth docs), dependency decision (implement ~400 documented lines; no new runtime dep), model/limit mapping, risk register, implementation plan.
- [x] **Auth flow** — `server/src/codex/` (`protocol`/`errors`/`oauth`/`connection`): "Connect ChatGPT" binds `127.0.0.1:1455/auth/callback` for ≤5 min, opens the browser, validates state, exchanges the PKCE code, and stores tokens in the OS keychain via the existing `SecretStore` (`chatgpt-auth`); the browser sees connection facts only (connected, email, plan). Singleflight refresh inside a 120 s margin; terminal refresh failures (OAuth `invalid_*`, bare 401/403) quarantine as auth-expired with fail-fast; 429/5xx stay retryable; redact-before-truncate on every error body. Disconnect deletes the record (no upstream revoke exists). Unit-tested against a mocked token endpoint and a real loopback listener (PKCE spec vector, state-mismatch, occupied port, timeout, quarantine, recovery).
- [x] **Provider** — `server/src/codex/provider.ts`: `createOpenAI(...).responses` with a wrapper `fetch` that forces `store:false/stream:true/parallel_tool_calls:false`, drops the backend-rejected parameters (incl. the harness's `max_output_tokens`), injects `include:["reasoning.encrypted_content"]` + `client_version` + subscription headers (one session UUID per provider), retries a 401 exactly once via forced refresh, and maps 429 → quota / second-401 → auth_expired / other non-2xx → endpoint_changed·transport, all typed and token-redacted. Tested end-to-end through the real AI SDK Responses provider against a mock Codex backend: streaming, a tool round-trip (`function_call` → local tool → `function_call_output`), and `doGenerate` SSE-collapse.
- [x] **Config surface** — `chatgpt` joined `ProviderId` (keys stay key-only via `PROVIDERS`/`SUBSCRIPTION_PROVIDERS`); three routes (`POST connect`, `GET status`, `DELETE disconnect`) + connection status embedded in `SetupStatus`; curated picker models (`gpt-5.4-mini/5.4/5.5/5.3-codex-spark`) gated on the connection with no network call. No new env vars.
- [x] **Web (three insertions)** — Setup gained a self-contained "ChatGPT subscription" row (Connect → poll → connected email/plan, Disconnect); ModelPicker groups under "ChatGPT (subscription)"; the chat error notice recognises `[coja:chatgpt:*]`, titles quota/auth/endpoint failures honestly, links "Reconnect ChatGPT in Setup", and offers the one-click key-model switch only when an API-key model is configured.
- [ ] **Live E2E** — browser-run Connect (login completed by the user in their Chrome), a subscription-backed chat with a tool call on the fixture PR, restart persistence, disconnect, and the repo-wide token-leak grep. In progress below.
- [x] **Live E2E (with the user approving in their Chrome)** — Connect completed on the real `auth.openai.com`; status reported `connected` with the account email and plan (`plus`); the picker listed the four curated `chatgpt:*` models with zero provider calls; a real chat on fixture PR #1 with **GPT-5.4 mini** answered via the subscription, and a tool-call turn executed `read_file(head, src/auth/session.ts:1-260)` and streamed the correct summary of `rotate()`; the conversation (tool parts included) survived a server restart from SQLite; Disconnect (via the Setup button) emptied the keychain item, flipped status to `idle`, gated the models off; a grep of the server log, SQLite (db + WAL) and the whole working tree for the live token substrings found **zero** occurrences — tokens lived only in the OS keychain.
- [x] **Two fixes the live run surfaced.** (1) `store:false` means the backend persists nothing, so echoing previous-turn items by server-side id (`msg_…`) is refused with `Item with id '…' not found`; the transport now strips `id` from every echoed input item (keeping `call_id`) and drops `item_reference` entries (`stripServerSideState`, regression-tested). The failure itself exercised the typed-error path in the real UI: honest endpoint-changed title, marker-stripped message, Retry/Dismiss, no fallback offer (no API key configured). (2) `ChatGptRow` read the connected state only from the sign-in poll, so a page reload showed "Connect" while already connected; it now treats `SetupStatus.chatgpt` as the source of truth and uses the poll only for an in-flight sign-in.
- [x] **Reconnect after the test disconnect** — one more Connect → approve → `connected` round-trip; the app is left in the working, connected state.
- [x] **Models + reasoning efforts are catalog-driven (user request, same run).** The picker no longer uses a static list: `GET /backend-api/codex/models` (live-probed) feeds slugs, display names and per-model `supported_reasoning_levels`; hidden entries (`gpt-reserve`, `codex-auto-review`) are filtered. TTL cache + settings-DB mirror + bootstrap fallback keep the picker alive offline. The AI panel gained a reasoning-effort selector (defaults to the model's catalog default, resets on model switch); the effort is validated against the catalog in the chat route and injected into the request body by the transport (bypassing the AI SDK's stale `ultra`-less enum). Tests +9 (catalog parse/filter, header/401-retry/typed errors, TTL + mirror fallback, effort injection incl. chosen-beats-default, catalog-driven setup models). Live: picker shows the Plus plan's five listed models; a GPT-5.4-Mini chat at `high` effort ran `read_file` and streamed the correct answer.
- [x] **Test-hygiene fix (user-reported): the suite was opening real browser tabs.** The OAuth-flow tests built `ChatGptConnection` without stubbing the `openBrowser` seam, so every `connect()` in a test run called the production default (macOS `open`) and pointed the host's browser at `auth.openai.com/oauth/authorize?...redirect_uri=http://localhost:<ephemeral freePort>/auth/callback` — the "dangling service" the user saw (the listener dies with the test; the tab stays). All test factories now stub `openBrowser` with a capturing no-op, and the main connect test asserts the authorize URL is opened exactly once — side effect turned into coverage.

## Custom model providers + setup polish (2026-09-05, later still)

- [x] **Custom model providers (user request: "not OpenAI/Anthropic-specific").** API-surface research: OpenAI's Chat Completions (stateless), Responses (stateful, reasoning items), Conversations (durable server-side threads) vs Anthropic's Messages (thinking built in). Decision: custom providers speak **Chat Completions** or **Anthropic Messages** at a user-chosen base URL — that covers DeepSeek, OpenRouter, Azure, self-hosted proxies and local Ollama/LM Studio; the stateful Responses/Conversations surfaces are deliberately skipped because coja owns conversation state locally (SQLite, full-transparency rendering) and vendor-side threads would add nothing but lock-in. Server: `server/src/custom-providers.ts` (settings-table store, zod validation, id = `custom-<slug(name)>`, upsert-on-same-id), key per provider in the SecretStore, `POST/DELETE /api/setup/custom-providers`, `SetupStatus.customProviders` (configs + `hasKey`, never key material), `/api/ai/models` includes each provider's hand-curated models (with `providerLabel` for display), and `resolveLanguageModel` builds `createOpenAI({ baseURL })` / `createAnthropic({ baseURL })` from the stored config. Web: an "Add model provider" form in Setup (Name / Base URL / API key optional / API format / hand-curated model list, ≥1 model required), configured list with Edit/Remove, and the AI panel's picker groups custom models under the provider's name. Browser-validated end to end on the user's instance: add → picker → edit (models change) → remove.
- [x] **Providers are editable after adding (user request).** The Edit button opens the same form pre-filled; the server upserts on the same id. Key semantics: a typed key stores/replaces, a blank key field **keeps** the stored one (it is never shown again, so it could not be re-entered); `hasKey` is surfaced so the form can say so. One server fix found by the route test: the DELETE path was registered through the API_ROUTES helper, which URL-encodes the `:id` marker — now registered as a raw segment.
- [x] **Setup/theme polish (user annotations).** Appearance is a real radio group (native inputs, sr-only) with a visible "Appearance" label, **System** instead of "Auto", and the explanatory paragraph removed; the palette grid has a "Palette" label (no longer a duplicated "Theme"). The "Skip AI for now" button is gone — Continue completes setup either way.
- [x] **Theme quick switcher removed from the top bars (user request)** — the palette picker lives in Setup only. `ThemeMenu` deleted; AppShell renders only the screen's own actions.
- [x] **Validation servers (session preference, see earlier note): all browser validation in this round ran against the user's instance on port 4321.** Final state: 700 tests green, build green, server code untouched by this round's UI work (server API gained only the Setup/provider routes).

- [x] **Primary nav in the header (user request: no more Setup/Projects back-and-forth).** AppShell's header now carries `Projects` + a settings gear leading to Setup, with the active screen highlighted (`bg-active` chip, `aria-current="page"`); the PR-list route counts as Projects. The old links — the "Setup" text link on Projects and "Back to projects" on Setup — are gone (the review screen keeps its "← PRs" back link, which is context, not navigation). Verified both directions in the browser.

- [x] **Built-in keyed providers removed (user decision, both agreed).** With custom endpoints plus the ChatGPT subscription, the OpenAI/Anthropic API-key row was redundant — a user adds "OpenAI" or "Anthropic" as a custom provider pointing at their official base URL. Removed end to end: `ProviderId`/`PROVIDERS`/Save-Key types and routes from the wire API, `validateApiKey`/`listModels`/`ModelCache` and the models-list narrowing from the server, the built-in branches from `parseModelId`/`resolveLanguageModel`/`defaultModelId` (custom providers + ChatGPT only; customs never default — the picker always sends an explicit model), `ProviderRow` and its hooks from the web, and all keyed tests. Setup is now: GitHub → ChatGPT subscription → Model providers (custom) → Theme. `secrets/providers.ts` shrank to the ChatGPT label + bootstrap catalog. 675 tests green.
- [x] **Theme quick switcher removed from the top bars (user request)** — the palette picker lives in Setup only. `ThemeMenu` deleted; AppShell renders only the screen's own actions.

- [x] **Refresh moved out of the header (user request).** The PR list's Refresh is now a ghost icon button beside the "N open" count in the heading row — disabled with an inline spinner while the refetch runs. The AppShell header carries only the settings gear (plus screen-specific actions when a screen genuinely needs them).

- [x] **Model entries carry id + display name (user request: "model list again should have id and display names — display names in the dropdown, id sent to the APIs").** The fetch-probe already returned Anthropic `display_name`s; they were dropped. Now: `CustomProviderModel { id, label? }` in configs (backward-compatible — old plain-string entries normalize on read), `AddCustomProviderRequest.models` accepts strings or `{ id, label }`, `/api/ai/models` label precedence is endpoint display name → provider name + raw id, and the Setup form's chips show the display name with the raw id muted (tooltip on hover). The AI panel's picker shows the label; the wire always gets the id. Labels merge in from "Fetch model list" without overwriting names you may have set. Live-verified against the user's real Z.AI endpoint: fetch pulled 11 real models with the stored key, saved, and the picker shows them under the ZAI group. Labels merge in from "Fetch model list" without overwriting names you may have set.

## Chat sidepanel redesign (2026-09-06, this run)

Live-verified against the ZAI provider on the built app. `pnpm check`-equivalent green (681 tests, +6 net after rewriting the panel suites).

- [x] **Redesign** (inspired by Zed/t3 chat panels, not copied): centered empty state ("What should we look at?" + wrapping suggestion pills); user turns as right-aligned accent bubbles; assistant turns as full-width prose with model + relative-time captions; animated three-dot working indicator; card-style composer (rounded-xl, focus ring, chips inside, transparent textarea, circular send button).
- [x] **History hardening.** Switching chats now always refetches the record (`staleTime: 0`) so a history click shows the server's truth, never a stale cache entry; a message queued for a not-yet-mounted chat is tied to that chat id and discarded on switching away (it can no longer land in the wrong conversation). Verified live: switching among 13 chats, both directions.
- [x] **Suggestion pills send directly** — one click creates the chat (when needed) and posts the message; no composer round-trip. Verified live with a ZAI turn that answered with tool calls.
- [x] **In-input streaming state + real abort.** While a turn is in flight the composer shows a "Generating…" dot indicator and a Stop button; the placeholder switches to "Generating — draft your next question…". Stop aborts end to end: client fetch → Hono request signal → `streamText` abortSignal → the provider call is cancelled (verified: an aborted 500-word generation froze in ~1.3 s, the persisted turn stopped growing, no server errors). The partial answer is persisted by design, so a stopped turn survives reload.
- [x] **No horizontal overflow at the 300 px panel minimum** — suggestion pills wrap (measured: 3 pills on 3 rows), chips wrap inside the composer card, message list is `overflow-x-hidden` with `break-words` content, and a 16 k-char conversation with 5 tool cards measured zero overflow on panel/list/document.
- [x] **"AI" wording removed from the panel**: header title gone (icon controls only: history clock with count badge, +, eye), "What does the AI see?" → "Context & tools", no-provider copy → "No model provider configured".

## Model picker modernised + moved to the composer (2026-09-06, later)

- [x] **Custom model picker replaces the native `<select>`**: a compact pill above the input card opening an *upward* dropdown — provider-grouped sections with uppercase headers, checkmark on the selected model, hover states, full keyboard navigation (arrows / Enter / Escape / Home / End), ARIA combobox+listbox wiring, click-away close, closes on selection. The reasoning-effort selector got the same treatment (`EffortPicker`, pill + descriptions inline); it appears only when the active model publishes efforts (ChatGPT subscription).
- [x] **Moved to the bottom** per the discussed layout: pickers sit in a slim row directly above the input card (inside the composer's footer, one border). The header now shows the current chat title on the left and keeps only the history / new chat / context icons on the right.
- [x] Tests: +6 picker tests (empty state, grouping + selected mark, click-select-close, keyboard nav incl. Escape-without-choosing, effort picker); the 10 AiPanel tests rewritten for the pill (open → click option instead of `select.change`). 689 total green; live-verified on the built app (24 models across 3 groups, upward menu, selection persists).
- [x] **Single working indicator (user feedback).** The turn-in-flight state (dots + rotating phrase) now lives only in the composer card; the duplicate dots bubble in the conversation was removed — once tokens arrive the list shows content anyway, and the empty state stays suppressed mid-turn so suggestions never flash during the first message. Test updated to pin "no indicator in the list, suggestions hidden while submitted".
- [x] **Tool pills modernised to match the messages (user feedback).** Tool calls render as soft rounded-xl pills (wrench icon, mono signature, status, `Result`/`Input` label, rotating chevron; the whole pill is the toggle) with the expandable payload in a matching rounded panel — failed calls auto-open so the error is seen. The Reasoning disclosure and context chips got the same soft language (rounded-xl, `bg-active` fill instead of hard bordered boxes). All existing aria hooks (`Tool call …` region, `Result`/`Input`, `details`) kept; 689 tests green, live-screenshot verified.
