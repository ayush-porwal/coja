You are working in the Coja repository. Implement the supplied CJ product icon and handwritten Lottie loaders, bundle and apply IBM Plex Sans and Maple Mono NF, and add typography preferences to the existing Settings page. Inspect the codebase and its instructions first, then implement and verify the changes rather than stopping at a plan.

Read `docs/brand/README.md`. The approved assets are in `web/public/brand/coja/`: the theme-specific SVGs and 40 Lottie JSON files under `themes/`, indexed by `manifest.json`. Use these exact designs. The J begins touching the C and ends in an open hanging hook; do not redraw it, change the lettering, or substitute a stock icon. The product is named Coja (the earlier “Cojo” wording was a typo). Preserve ordinary brand copy and error messages.

1. Inspect the current working tree and preserve all unrelated staged and unstaged work. Read the app shell, boot flow, theme system, loading components, and package scripts. Search case-insensitively for Coja/Cojo, splash states, loading fallbacks, and existing product icons. Known starting points are `web/src/ui/AppShell.tsx`, `web/src/boot/BootGuard.tsx` (its Splash currently displays “coja”), `web/src/themes/ThemeContext.tsx`, `web/src/themes/apply.ts`, and `web/index.html`. Verify current contents rather than assuming they are unchanged.

2. Create a reusable static product-mark component from the supplied SVG geometry and use it in the existing product identity positions, including the app shell and favicon. Keep appropriate accessible names. Make the favicon legible at browser-tab size, with square framing and no clipping. Use the existing theme system to update both app icon and favicon when the palette or appearance changes. Do not replace normal text mentions of Coja with icons.

3. Create a reusable Lottie loader with huge (360px), large (160px), medium (64px), and small (24px) sizes, responsive sizing, and optional status label. Use the huge loader centered in the full-page boot splash and choose the other sizes to fit existing loading contexts. Replace branded Coja/Cojo loading text wherever it represents a loading placeholder. Preserve useful loading labels, meaningful skeletons, progress indicators, and button behavior unless they actually use the branded loading text being replaced. Do not introduce artificial loading delays or block rendering until an animation finishes.

4. Preserve the supplied slow-writing motion: C, then hanging J, then dot, hold, fade, repeat while loading. Use an appropriate installed Lottie runtime or add a compatible dependency through the repo's package manager if necessary. Use the SVG renderer with alpha track matte and trim-path support. Destroy player instances and remove listeners on cleanup; avoid duplicate instances under React Strict Mode. Do not load the entire animation collection eagerly. Handle runtime/asset failure with the static mark and accessible loading text so the UI never remains blank.

5. REQUIRED: switch BOTH the product logos (including favicon) AND loading animations based on the selected PALETTE AND resolved APPEARANCE. This is not just a light/dark toggle. Support Mulberry, Grove, Ocean, Ember, and Iris, each in light and dark. Load `web/public/brand/coja/manifest.json`; select `themes[palette.id][appearance].icon` for the icon and `themes[palette.id][appearance].loaders[size]` for the animation. Assets are under `/brand/coja/themes/{palette}/{light|dark}/cj.svg` and `cj-{huge|large|medium|small}.json`. These assets already contain the theme's ink/accent colors; do not use the old generic two-color variants for every palette or recolor the reveal matte. Read `useTheme()` for live selection and the existing registry helpers for persisted startup preference. Resolve system appearance through the existing theme system; system is not an asset directory. Respect an explicitly chosen app appearance over the OS appearance. Update logo, favicon, and every mounted loader immediately on palette/appearance changes, preserving playback position where practical. Avoid stale-load races when users switch rapidly. Prevent a wrong-theme flash at boot. For an unknown palette use the registry's default palette with the correctly resolved appearance. Do not eagerly load all 40 animations. If palette tokens change later, regenerate the asset matrix consistently rather than letting static icons and animations diverge.

6. Honor prefers-reduced-motion with the completed static mark (frame 260 or the supplied SVG), including live preference changes. Announce loading once through an appropriate status/live region; keep the animation decorative, and do not announce every frame. Maintain layout stability, narrow-screen fit, and unblocked keyboard interaction.

7. REQUIRED TYPOGRAPHY: make IBM Plex Sans the default UI font and Maple Mono NF (the Nerd Fonts variant of Maple Mono) the default code/monospace font. This font migration is explicitly in scope. Apply IBM Plex Sans consistently to navigation, headings, forms, buttons, dialogs, settings, and prose. Apply Maple Mono NF to diffs, source/code viewers, editors or terminals if present, inline code, fenced Markdown code blocks, and other existing monospace technical content. Inspect third-party components and shadow-root styling APIs (including Pierre diff/tree components) so they receive the right family; do not assume global CSS penetrates shadow DOM. Keep filenames and ordinary labels in the UI font unless the existing design intentionally treats them as code. Preserve the approved SVG logo geometry; do not regenerate it as font glyphs.

8. BUNDLE THE FONTS: inspect existing font assets first. Obtain missing fonts from official upstream releases, verify the exact family/variant, and save the redistributable webfont files locally in the repository (for example `web/public/fonts/ibm-plex-sans/` and `web/public/fonts/maple-mono-nf/`). Do not use Google Fonts/CDN requests, remote CSS imports, or rely on fonts being installed on the user's machine. Prefer WOFF2; use supported variable files where available or only the static weights/styles actually used. Include real italic faces where needed and correct weight/style declarations, avoiding synthetic bold/italic. Preserve the Maple Mono NF private-use Nerd Font glyphs and programming ligatures; do not silently substitute unpatched Maple Mono or an ASCII-only subset. Include each upstream license and a small provenance note with release version and source URL. If conversion/subsetting is necessary, confirm license permission and preserve supported text/glyph coverage. Define local @font-face rules with font-display: swap, sensible sans/monospace fallbacks, and centralized typography tokens; preload only critical defaults when justified. Document asset sizes and check that built output includes the fonts and works without external font requests.

9. TYPOGRAPHY SETTINGS: extend the existing Settings page and persistence patterns with a clearly labeled Typography section. Provide separate UI and code font-family selectors (IBM Plex Sans / System sans for UI; Maple Mono NF / System monospace for code), UI font size, code font size, code line height, and a code-ligatures toggle. Use existing type sizes as the initial defaults, choose sensible bounded ranges, and show the current values and units. Keep the choices useful rather than listing unavailable fonts. Include a live preview with ordinary UI text and code such as `const answer = input !== null ? input => input + 1 : 0;`, plus a few actual supported Nerd Font glyphs with accessible text labels. Changes should apply immediately across mounted views, including diffs and code blocks, without a reload. Persist preferences across sessions using the project's existing approach; validate stored data, recover from invalid/obsolete values, and apply persisted settings early enough to avoid a conspicuous typography flash. Keep typography independent of color palette/appearance so changing themes never resets font preferences. Provide Reset typography defaults, affecting only typography. Make controls keyboard-accessible with associated labels and preserve browser zoom. Recompute virtualized row measurements or use supported component APIs when font metrics change so code/diff rows do not overlap or become misaligned. Avoid a second settings page or unrelated settings redesign.

10. Run the relevant repository checks and add focused tests for behavior that matters: loading-state replacement, size selection, theme updates, reduced motion, and player cleanup/fallback. Verify the actual animation in a browser at full-page and small sizes, all five palettes in both light and dark appearance, plus system mode and persisted startup selection. Inspect intermediate writing frames as well as the completed mark to catch matte or path-reveal errors. Confirm errors and retry actions remain usable and unrelated loading patterns have not regressed. Add focused typography tests for preference validation/persistence/reset, immediate application, and independence from theme changes. In a browser verify that the bundled fonts actually load (not just that CSS names them), that no external font request occurs, and that code ligatures toggle correctly. Check real Nerd Font glyph rendering, fallback behavior, narrow layouts, browser zoom, and diff alignment/virtualization after font size and line-height changes. Run the production build and inspect its font asset URLs.

Finish with a concise summary of integration points, validation results, and any limitations. Do not commit, publish, or deploy unless asked.
